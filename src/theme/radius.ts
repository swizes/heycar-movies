import metrics from '@/theme/metrics';

export default {
  xs: 4 * metrics.verticalRem,
  s: 8 * metrics.verticalRem,
  m: 16 * metrics.verticalRem,
  l: 24 * metrics.verticalRem,
  xl: 40 * metrics.verticalRem,
};
